import { StyleCardBackgroundDirective } from './style-card-background.directive';

describe('StyleCardBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleCardBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
